import {Extension} from 'resource:///org/gnome/shell/extensions/extension.js';

export default class AppAndExtensionSchedulerExtension extends Extension {
    enable() {
    }

    disable() {
    }
}
